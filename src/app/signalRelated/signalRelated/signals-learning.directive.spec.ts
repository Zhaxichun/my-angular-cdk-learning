import { SignalsLearningDirective } from './signals-learning.directive';

describe('SignalsLearningDirective', () => {
  it('should create an instance', () => {
    const directive = new SignalsLearningDirective();
    expect(directive).toBeTruthy();
  });
});
